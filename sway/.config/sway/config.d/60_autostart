# ==========================================
# AUTOSTART — Daemons & startup applications
# ==========================================

# --- CORE SYSTEM (Wayland & Polkit) ---
# exec /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
# (sway-systemd-git handles dbus and sway-session.target automatically)
exec systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP SWAYSOCK && hash dbus-update-activation-environment 2>/dev/null && dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP SWAYSOCK
exec bash -c 'sleep 1 && systemctl --user restart xdg-desktop-portal-wlr.service xdg-desktop-portal.service'
# exec gnome-keyring-daemon --start --components=secrets


# --- BACKGROUND DAEMONS ---
# exec udiskie

    
# # exec bash ~/.config/sway/scripts/workspace_osd.sh &
# # exec bash ~/.config/sway/scripts/app_osd.sh &
# exec autotiling
# exec fcitx5 -d


# --- DankMaterialShell ---
exec systemctl --user start dms.service
# exec valent --gapplication-service


# --- AUTOSTART APPS ---
exec bash -c 'sleep 1 && swaymsg "workspace 1" && (footclient || foot)'
